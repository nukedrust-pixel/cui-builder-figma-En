{
  "name": "figma-to-rust-ui-exporter",
  "version": "1.0.0",
  "description": "Плагин для экспорта UI из Figma в Rust код",
  "main": "code.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "@figma/plugin-typings": "^1.50.0",
    "typescript": "^4.6.3"
  }
} 